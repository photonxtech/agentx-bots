# CDK toolchain only — kept separate from the application's requirements.txt so
# deploying never perturbs the application's load-bearing langchain pins.
aws-cdk-lib==2.265.0
constructs>=10.0.0,<11.0.0
