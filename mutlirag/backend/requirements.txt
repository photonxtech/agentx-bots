# --- Backend API + frontend server (FastAPI serves the HTML UI) ---
fastapi>=0.115
uvicorn[standard]>=0.30
python-multipart>=0.0.9   # required by FastAPI for file uploads

# --- LLM generation (Groq) ---
groq>=0.11
python-dotenv>=1.0

# --- Embeddings (local, free, no API key) ---
sentence-transformers>=3.0

# --- Retrieval (hybrid: NumPy cosine + BM25 keyword) ---
rank-bm25>=0.2

# --- Excel/CSV ingestion ---
pandas>=2.2
openpyxl>=3.1

# --- Image OCR ---
easyocr>=1.7
Pillow>=10.0

# --- PDF (text extraction + render-to-image for scanned pages) ---
pymupdf>=1.24

# --- Word .docx (text + tables + embedded images) ---
python-docx>=1.1

# --- PowerPoint .pptx (slides + tables + embedded images) ---
python-pptx>=1.0

# --- Vector DB ---
# Weaviate is the default backend (self-hosted via docker-compose.yml).
weaviate-client>=4.9,<5
# Chroma kept as an automatic fallback if Weaviate is unreachable.
chromadb>=0.5.0

# --- Utils ---
numpy>=1.26

# --- Q&A + RAGAS metrics logging (Postgres) ---
psycopg2-binary>=2.9
