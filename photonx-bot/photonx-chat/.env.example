# Backend URLs the UI can switch between (toggle in the header).
# Defaults match the two Python backends running locally.
VITE_MANUAL_URL=http://localhost:8000
VITE_LANGCHAIN_URL=http://localhost:8001
